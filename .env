# Camera stream url (rtsp://... or local file like t3.mp4 for testing)
RTSP_URL=t6.mp4
# Where the event engine writes log lines
LOG_FILE_PATH=logs/events.log
# Seconds to wait before reconnecting to the camera
RECONNECT_DELAY=5
# Every N seconds each stage logs its actual frames-per-second
STAT_INTERVAL=15
# Moondream 2 (2B) via Hugging Face — shared by all detectors
MOONDREAM_MODEL_ID=vikhyatk/moondream2
MOONDREAM_DEVICE_MAP=cpu
MOONDREAM_HF_REVISION=2025-06-21
# Municipal assets (Moondream query + grounding)
ASSET_FOV=
ASSET_PROMPTS=street sign,bollard,barrier,street lamp,fire hydrant
ASSET_RECORD_DIR=records/assets
# Vehicle movement (unused if only asset detection is enabled)
VEHICLE_FOV=
VEHICLE_RECORD_DIR=records/vehicles
# Annotated output: empty path + AUTO_SAVE=true names it after the input
OUTPUT_VIDEO_PATH=
OUTPUT_AUTO_SAVE=true
OUTPUT_FPS=10
OUTPUT_SHOW=false
OUTPUT_PREVIEW_WIDTH=1280
OUTPUT_PREVIEW_HEIGHT=720

# Alert engine email settings
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
ALERT_TO=
ALERT_ENABLED=true
